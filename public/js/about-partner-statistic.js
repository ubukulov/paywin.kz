const uploadStatCard = document.querySelector('#upload-stat-card'),
      uploadStatCardBtn = document.querySelector('.partner__statistic-card-upload');

uploadStatCardBtn.addEventListener('click', () => {
    uploadStatCard.click();
});