[www]
; Nginx emits the structured access record with the original request URI.
; Disable FPM's duplicate /index.php-only access line.
access.log = /dev/null

catch_workers_output = yes
decorate_workers_output = no
